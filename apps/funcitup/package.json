{
  "name": "funcitup",
  "version": "1.0.0",
  "description": "Example app for Azure Functions with TypeScript and custom decorators",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "prebuild:prod": "pnpm run clean && pnpm typebox-generator -r src",
    "build:prod": "esbuild --tsconfig=./tsconfig.json src/functions/index.ts --bundle --minify --platform=node --target=node20 --outdir=./dist/src/functions --external:@azure/functions-core",
    "prebuild": "pnpm run clean && pnpm typebox-generator -r src",
    "build": "tsc",
    "predeploy": "node -e 'var p=require(\"./package.json\");p.scripts=p.dependencies=p.devDependencies=void 0;p.main=\"src/functions/index.js\";console.log(JSON.stringify(p,null,2))' > dist/package.json && cp host.json local.settings.json dist",
    "deploy": "pushd dist; func azure functionapp publish devFunc001; popd",
    "postdeploy": "rimraf dist/host.json dist/local.settings.json",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "pnpm run clean && pnpm run build:prod",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@sinclair/typebox": "^0.34.13",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "decorators": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "azure-functions-core-tools": "^4.x",
    "esbuild": "^0.24.2",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.2",
    "typebox-generator": "workspace:^",
    "typescript": "^5"
  }
}
